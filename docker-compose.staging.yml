services:
  fastapi:
    container_name: fireseed-trilogy-fastapi-staging
    ports:
      - "8001:8000"
    environment:
      - ENV=staging
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8000/metrics || exit 1"]
      interval: 30s
      timeout: 3s
      retries: 5
