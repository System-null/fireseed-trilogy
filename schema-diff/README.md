# Schema Diff
Place human-readable diffs here when schemas evolve.

## Generate example
```bash
pip install jsondiff
jsondiff schemas/FireseedCapsule_v0.json schemas/FireseedCapsule_v1.json > schema-diff/v0_to_v1.diff
```
